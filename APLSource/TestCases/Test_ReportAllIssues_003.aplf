 R←Test_ReportAllIssues_003(stopFlag batchFlag);⎕TRAP;stop;rc;msg;data
⍝ Exercise ]GitHub.ReportAllIssues without any parameters
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 stop←0
 (rc msg data)←stop UCMD'ReportAllIssues -assignees=aplteam -raw'
 →T.PassesIf rc=0
 data←↑,[1]/2↓¨((≢data)⍴0 1)/,data
 →T.PassesIf 0∧.<≢¨⊢/data

 R←T._OK
⍝Done
