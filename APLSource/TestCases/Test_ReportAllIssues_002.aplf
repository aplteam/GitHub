 R←Test_ReportAllIssues_002(stopFlag batchFlag);⎕TRAP;stop;rc;msg;data
⍝ Exercise ]GitHub.ReportAllIssues without any parameters
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 stop←0
 (rc msg data)←stop UCMD'ReportAllIssues -raw'
 →T.PassesIf rc=0
 →T.PassesIf 2=⍴⍴data

 R←T._OK
⍝Done
