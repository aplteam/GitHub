 R←Test_Misc_011(stopFlag batchFlag);⎕TRAP;name;txt
⍝ Regression test: level-0 help (]cmd -?) must not be empty for any command.
⍝ `ReportAllIssues` used to have no :Case at all, so it printed nothing.
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :For name :In ##.GitHub.Public,⊂'Version'
     txt←0 ##.GitHub.Help name
     →T.PassesIf 0<≢txt
 :EndFor

 R←T._OK
⍝Done
